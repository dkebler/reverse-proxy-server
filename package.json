{
  "name": "reverse-proxy",
  "version": "1.0.0",
  "description": "A reverse proxy server based on redbird",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dkebler",
  "license": "MIT",
  "dependencies": {
    "redbird": "^0.7.0"
  }
}
